import { SwipeMenuDiractive } from './swipe-menu-diractive';

describe('SwipeMenuDiractive', () => {
  it('should create an instance', () => {
    const directive = new SwipeMenuDiractive();
    expect(directive).toBeTruthy();
  });
});
