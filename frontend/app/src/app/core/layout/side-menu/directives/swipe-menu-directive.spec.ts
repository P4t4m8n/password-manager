import { SwipeMenuDirective } from './swipe-menu-directive';

describe('SwipeMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeMenuDirective();
    expect(directive).toBeTruthy();
  });
});
